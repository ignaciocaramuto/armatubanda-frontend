import { ConvertImageToFilePipe } from './convert-image-to-file.pipe';

describe('ConvertImageToFilePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertImageToFilePipe();
    expect(pipe).toBeTruthy();
  });
});
